# README Professional Upgrade Report

## نطاق العمل

تحويل `README.md` من ملف شرح تقني إلى صفحة Landing Page كاملة موجهة للمبتدئين، مع
إضافة رخصة استخدام حرة (MIT + إهداء عربي "لوجه الله")، وتحديث الصورة الرئيسية
(Hero) لتعكس هوية بصرية أوضح. تمت مراجعة الملفات التالية قبل الكتابة لضمان عدم
اختراع أي ميزة غير موجودة فعليًا: `SKILL.md`, `CODEX.md`, `rules/*.md`,
`templates/README.md`, `checklists/*.md`, `validators/README.md`, `schemas/*.json`,
`profiles/`, `examples/fixtures/`, `pyproject.toml`, و`tests/acceptance/test_readme_integrity.py`
(لمعرفة القيود الآلية المفروضة على شكل README قبل الكتابة).

## الملفات التي تم تعديلها

| الملف | التعديل |
|---|---|
| `README.md` | إعادة كتابة كاملة كصفحة Landing Page عربية (تفاصيل أدناه). |
| `assets/readme/hero.svg` | تحديث الألوان لتقارب الباليت المطلوب (كحلي/ذهبي/عاجي/رمادي مزرق)، وإضافة العنوان الإنجليزي "Arabic Legal Research Skill" بجانب "arabic-legal-research-skill" والعنوان العربي "مهارة البحث القانوني العربي". |
| `pyproject.toml` | إضافة حقل `license = "MIT"` فقط (توافق Metadata مع ملف LICENSE الجديد؛ لا تغيير في أي سلوك تنفيذي). |

## الملفات التي تم إنشاؤها

| الملف | الغرض |
|---|---|
| `LICENSE` | نص رخصة MIT الرسمي الكامل غير المعدَّل، مسبوقًا بمقدمة عربية محترمة توضح أن المشروع صدقة علمية "لوجه الله"، مع طلب الحفاظ على نسبة الفضل، وتنبيه صريح أن المشروع لا يغني عن محامٍ مختص. |
| `README_PROFESSIONAL_UPGRADE_REPORT.md` | هذا التقرير. |

**لم يُحذف أي محتوى مهم**؛ الأقسام التقنية التفصيلية (أوامر CLI، حالات النتيجة، Exit codes، بوابة Word) نُقلت داخل قسم `<details>` قابل للطي تحت "Workflow" بدل حذفها، حفاظًا على ثراء المحتوى دون إرهاق القارئ المبتدئ في أول قراءة.

## ملخص تصميم README الجديد

البنية الجديدة (بالترتيب):

1. **Hero Section**: صورة SVG + عنوان مزدوج (عربي/إنجليزي) + جملة تعريفية + تنبيه فوري أن الأداة لا تغني عن محامٍ.
2. **Badges احترافية**: CI حقيقي، Python، الإصدار، عدد الاختبارات الفعلي (233 ناجح)، التغطية الفعلية (96.12%)، شارات لغوية/تصنيفية (Arabic، AI Skill، DOCX)، وشارة License تعكس MIT الفعلية. **لا توجد شارة واحدة غير حقيقية** — كل رقم مأخوذ من نتيجة تشغيل فعلي لـ `pytest` في هذه الجلسة.
3. **"ما فكرة المشروع ببساطة؟"**: شرح مفهوم Skill، سبب الحاجة لتعليمات ثابتة، المشكلة المحلولة، وجدول يوضح الفرق بين الاستخدام كرابط شات والاستخدام كملفات محلية.
4. **"لمن هذا المشروع؟"**: جدول بست فئات (طالب، باحث، محامٍ، مطوّر، مستخدم عادي، مستخدم Codex/Claude/ChatGPT) مع فائدة كل فئة.
5. **"ماذا يستطيع المشروع أن يفعل؟"**: بطاقات (Cards) لأربع مجموعات قدرات حقيقية، مع تنبيه صريح مكرر أن المشروع لا يثبت صحة الرأي القانوني ولا يقدم مراجعة بشرية.
6. **الاستخدام بدون تثبيت**: خطوات + Prompt جاهز للنسخ المباشر في ChatGPT/Claude.
7. **الاستخدام مع Claude Code**: خطوات `git clone` + فتح Claude Code + Prompt جاهز بالإنجليزية (يطابق النص الذي طلبه المستخدم حرفيًا).
8. **الاستخدام مع OpenAI Codex**: خطوات مكافئة + Prompt جاهز (يطابق النص المطلوب حرفيًا). تم توضيح أن Codex لا يملك "متجر Skills" مطابقًا لـ Claude، وأن `CODEX.md` و`SKILL.md` هما البديل العملي — تجنبًا لأي ادعاء غير دقيق عن آلية غير موجودة.
9. **التثبيت للمطورين**: خطوات `venv` + `pip install -e ".[dev]"` + `pytest`، مطابقة تمامًا لما هو موجود فعليًا في `pyproject.toml` ومُختبرة حرفيًا في هذه الجلسة (تم تشغيلها بنجاح).
10. **هيكل المشروع**: شجرة + جدول وظائف، مع الإشارة الصريحة إلى أن `templates/` لا يحتوي حاليًا ملفات `.docx` جاهزة (حقيقة مأخوذة من `templates/README.md` نفسه) — لتفادي أي انطباع خاطئ.
11. **أمثلة استخدام عملية**: ستة طلبات جاهزة واقعية.
12. **Workflow**: مخطط Mermaid مبسط للمبتدئين + مخطط تفصيلي إضافي (تنفيذي) داخل قسم قابل للطي.
13. **الاختبارات والجودة / الأمان**: أرقام حقيقية محدثة (233/1/96.12%)، وملخص لضمانات الأمان الفعلية (لا `eval`/`exec`، لا قتل عمليات Word عامة، GitHub Actions مثبّتة بـ commit SHA).
14. **الرخصة والاستخدام المجاني (لوجه الله)**: قسم مستقل صريح يذكر عبارة "لوجه الله" بوضوح، يشرح الشروط المسموحة والممنوعة، ويحيل لملف `LICENSE`.
15. **Footer**: بقي **دون أي تغيير** في الترتيب أو النص أو الروابط (Instagram → LinkedIn → Facebook → WhatsApp → GitHub)، مطابقًا تمامًا للنسخة السابقة، وهذا مؤكَّد آليًا عبر اختبار `test_readme_footer_links_and_signature_are_exact` الذي ما زال ينجح دون أي تعديل عليه.

### الألوان المستخدمة

اعتُمدت شارات (Badges) وعناصر HTML بألوان قريبة من الباليت المطلوب: كحلي `#1E3A8A`/`#0B132B`، ذهبي `#C9A227`، رمادي مزرق `#334155`، إلى جانب الألوان الوظيفية الأصلية (أخضر للاختبارات الناجحة). صورة الـ Hero نفسها أعيد تلوينها بنفس الباليت بالضبط (`#0B132B`, `#C9A227`, `#F8F5EC`).

## طريقة التثبيت الموثّقة

خطوات مطابقة حرفيًا لما هو مذكور في `pyproject.toml` (تم تشغيلها فعليًا في هذه الجلسة والتأكد من نجاحها):

```bash
git clone https://github.com/Darhous/arabic-legal-research-skill.git
cd arabic-legal-research-skill
python -m venv .venv
.venv\Scripts\activate      # Windows
source .venv/bin/activate   # macOS/Linux
python -m pip install -U pip
pip install -e ".[dev]"
pytest
```

## طريقة الاستخدام في ChatGPT / Claude / Codex

الثلاث طرق موثقة بالكامل في README الجديد بخطوات مرقّمة وPrompt جاهز لكل واحدة
(أقسام: "استخدامه داخل ChatGPT / Claude بدون تثبيت"، "الاستخدام مع Claude Code"،
"الاستخدام مع OpenAI Codex") — راجع الأقسام المذكورة مباشرة في `README.md`.

## حالة LICENSE

تم إنشاء ملف `LICENSE` جديد في جذر المستودع، يحتوي:

1. مقدمة عربية (~8 أسطر) تبدأ بـ"بسم الله الرحمن الرحيم"، تشرح أن المشروع صدقة
   علمية مجانية "لوجه الله"، وتوضح شروط الاستخدام والتعديل والنشر، وتطلب فقط
   الحفاظ على نسبة الفضل، مع تنبيه أن المشروع لا يُستخدم للتضليل ولا يغني عن
   محامٍ مختص.
2. نص رخصة **MIT الرسمي الكامل غير المعدَّل** (Copyright (c) 2026 Ahmed Darhous)،
   لضمان بقائها رخصة مفتوحة المصدر معترف بها قانونيًا وتقنيًا (بما يشمل احتمال
   اكتشاف GitHub التلقائي لها كـ MIT).
3. أُضيف حقل `license = "MIT"` إلى `pyproject.toml`، وأُعيد تثبيت الحزمة محليًا
   للتأكد أن Metadata الحزمة أصبحت تعكس ذلك فعليًا
   (`License-Expression: MIT` ظهر في `importlib.metadata` بعد إعادة التثبيت).
4. تم **حذف** التحذير القديم في README ("لا يوجد ملف LICENSE... جميع الحقوق
   محفوظة") لأنه لم يعد صحيحًا، واستُبدل بقسم "الرخصة والاستخدام المجاني (لوجه الله)"
   الجديد الذي يشرح الوضع الحالي بدقة.

## حالة الصور والـ Assets

- تم تحديث `assets/readme/hero.svg` في مكانه الأصلي (بدل إنشاء نسخة مكررة في
  مسار مختلف) لتفادي أي كسر لاختبار `test_readme_hero_exists_and_svg_is_safe`
  الذي يتحقق من هذا المسار تحديدًا. الصورة تحتوي الآن على العنوانين العربي
  والإنجليزي معًا، اسم المطوّر، والباليت اللونية المطلوبة.
- لم يتم إنشاء `workflow.svg` منفصل: مخطط Mermaid المُضمَّن في README يؤدي نفس
  الغرض ويُعرض بشكل أصلي على GitHub دون الحاجة لملف صورة إضافي — قرار متعمد
  لتفادي ازدواجية غير ضرورية.
- لا صور خارجية أو روابط مكسورة: كل الصور والروابط النسبية داخل README تشير
  لملفات موجودة فعليًا داخل المستودع، ومؤكَّدة آليًا عبر
  `test_readme_relative_links_exist_and_no_local_artifact_paths`.

## نتائج الاختبارات

شُغّلت الاختبارات فعليًا في هذه الجلسة بعد كل تعديل جوهري:

- `tests/acceptance/test_readme_integrity.py`: **5/5 ناجحة** (Hero SVG آمن،
  الفوتر مطابق حرفيًا، الروابط النسبية صحيحة، أوامر CLI المذكورة كلها أوامر
  حقيقية، لا توجد ادعاءات إنجليزية ممنوعة).
- `python -m compileall src`: نظيف.
- `ruff format --check .` و`ruff check .`: نظيفان بالكامل.
- **الاختبار الكامل (`pytest`)**: **233 ناجح، 1 متجاوَز (skip)، 0 فاشل**،
  بتغطية **96.12%** (الحد الأدنى الرسمي في المشروع 95%).

## ملاحظات وقيود متبقية

- **لم يتم إنشاء GitHub Release** ولم تُنشأ أي Tag جديدة — وفق التعليمات
  الصريحة، هذه الخطوة متروكة لصاحب المشروع يدويًا.
- **لم يُدفع (push) أي شيء بعد** إلى GitHub — التعديلات موجودة محليًا فقط في
  هذه اللحظة، بانتظار تأكيد صاحب المشروع قبل أي `git push` (إجراء يؤثر على
  حالة عامة/مشتركة على GitHub).
- قسم "التثبيت للمطورين" وأقسام الـ CLI التفصيلية تفترض أن القارئ يريد تشغيل
  أدوات التحقق التنفيذي الفعلية؛ الاستخدام "بدون تثبيت" عبر الشات العادي لا
  يحتاج أيًا من هذه الخطوات، وهذا موضّح صراحة في الجدول المقارن أول القسم
  التعريفي.
- لم يُغيَّر أي كود تنفيذي داخل `src/legal_research_skill/` في هذه المهمة
  سوى إضافة حقل `license` إلى `pyproject.toml` (تعديل Metadata بحت، مطلوب
  لعرض الرخصة بشكل صحيح، ولا يغيّر أي سلوك تشغيلي — تم التحقق عبر تشغيل كامل
  حزمة الاختبارات بعده بنجاح تام).
