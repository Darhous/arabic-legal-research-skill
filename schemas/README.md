# Schemas

This directory contains the executable JSON Schema contracts introduced in Phase 3.

- `research-state.schema.json` validates structured task memory before domain models are built.
- `validation-report.schema.json` validates machine-readable reports emitted by the pipeline.

Both schemas use JSON Schema Draft 2020-12 and schema version `0.3.0`.
